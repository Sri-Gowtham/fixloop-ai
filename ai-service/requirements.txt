# ================================================================
# requirements.txt — FixLoop AI  ·  ai-service
# ================================================================
# Core framework
fastapi==0.111.1
uvicorn[standard]==0.30.1

# Data validation
pydantic==2.7.4
pydantic-settings==2.3.4

# Supabase / PostgreSQL
supabase==2.5.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# pgvector Python client
pgvector==0.3.2

# OpenAI (embeddings + LLM)
openai==1.35.3

# HTTP client
httpx==0.27.0

# Async support
anyio==4.4.0

# Data processing
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0          # DBSCAN fallback + preprocessing
hdbscan==0.8.38              # primary clustering algorithm

# Google Gemini (optional — set CLUSTER_LLM_BACKEND=gemini to activate)
google-generativeai==0.7.2

# File parsing (CSV / JSON / NDJSON ingestion)
python-multipart==0.0.9
aiofiles==23.2.1
orjson==3.10.5

# Utilities
python-dotenv==1.0.1
structlog==24.2.0
tenacity==8.3.0              # retry logic for LLM / embed calls

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0                # also used as async test client

# Type stubs (dev)
types-aiofiles==23.2.0.20240403
